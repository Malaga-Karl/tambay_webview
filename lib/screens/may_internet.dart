import 'package:flutter/material.dart';

class MayInternet extends StatelessWidget {
  const MayInternet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text("May Internet")));
  }
}
